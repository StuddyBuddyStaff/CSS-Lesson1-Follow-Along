# Looking at CSS With and Without styles

## Without Styles Applied

As you look over this, you'll want to make sure that anything with styles applied is commented out to start. To apply comments to these, highlight what you wish to comment out and hit `CTRL` + `/` on Windows or `CMD` + `/` on MAC. You can place an `x` in each box to make sure you've followed the right steps.

- [ ] The link connecting `./css/styles-one.css`.
- [ ] The link connecting `./css/styles-two.css`.
- [ ] The `<style></style>` tag inside the `<head></head>` tag.
- [ ] The `<p id="first" class="text red" style="color: red">` tag.
- [ ] Next, hit the `Go Live` button in the bottom of your VS Code to open it in the browser.
- [ ] To open the Inspect Tool on your browser on Windows, click `CTRL`, `SHIFT` and `I`.
- [ ] To open the Inspect Tool on your browser on MAC, click `CMD`, `SHIFT` and `I`.
- [ ] Look at the default styles being applied to the elements on the `Index.html` file.

### With the Style Tags Applied

- [ ] Remove the comments from the `<style></style>` tag in the `<head></head>` tag.
- [ ] Look at the Inspect tool to see what is being applied.

### With the Inline Style Tags Applied

- [ ] Remove the comments from the `<p id="first" class="text red" style="color: red">` tag.
- [ ] Comment out the `<p>` tag above the `<p id="first" class="text red" style="color: red">` tag
- [ ] Look at the Inspect Tool to see what is being applied now.

### With the CSS Style One File Applied

- [ ] Remove the comments from the `./css/styles-one.css` link in the `<head></head>` tag.
- [ ] Look at the Inspect Tool to see what is being applied now.

### With the CSS Style Two File Applied

- [ ] Remove the comments from the `./css/styles-two.css` link in the `<head></head>` tag.
- [ ] Look at the Inspect Tool to see what is being applied now.
